# Add a simple pipeline without using builder template.
add_simple_pipeline

# Add a duplicate pipeline to trigger error handling
add_duplicate_pipeline

# Add a pipeline for Go template without using builder pattern.
add_go_template_pipeline

# Add pipeline material and fetchartifact to downstream pipeline as we create upstream pipeline
# State everything explicitly
add_downstream_dependency

# Add pipeline material and fetchartifact to downstream pipeline as we create upstream pipeline
# Imply what's not stated
add_downstream_dependency_implicit

# Add pipeline with upstream parameter in template
add_pipeline_with_upstream_parameter_in_template

# Test plugin system for custom functionality
# (pretty much a clone of add_simple_pipeline, but implemented through minimal plugin)
plugin_test
